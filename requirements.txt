# ── Core (Vector Store & Embeddings) -------------------------------------
chromadb==1.5.9  # Pin client/server for the v0.6 clean rebuild
openai>=1.0.0

# ── Legacy support (for backward compatibility) ---------------------------
# Note: These are optional - only needed if using local sentence-transformers
# faiss-cpu>=1.7.0
# torch>=2.0.0
# sentence-transformers>=2.0.0
# transformers>=4.30.0
# huggingface-hub>=0.20.0
# tokenizers>=0.15.0

# ── Data handling ---------------------------------------------------------
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── Streamlit UI & API ----------------------------------------------------
streamlit>=1.30.0
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# ── I/O & utilities -------------------------------------------------------
requests>=2.28.0
tqdm>=4.65.0
PyYAML>=6.0
rich>=13.0.0
python-dotenv>=1.0.0

# ── PDF & doc extraction --------------------------------------------------
# v0.6 extraction route (see docs/EXTRACTION_QUALITY_GATE.md): Zotero FT cache +
# pdfminer cover ~99% of the corpus; Marker+OCR is the rare escalate backstop.
# Docling and pymupdf4llm were evaluated and dropped — Docling was only
# pdfminer-grade (whitespace noise) and pymupdf4llm failed scans without OCR.
# They can be re-added behind the extractor seam if a future fixture class needs them.
marker-pdf==1.10.2
PyMuPDF>=1.23.0
python-docx>=1.0.0
html2text>=2020.1.16
pdfminer.six>=20221105
Markdown>=3.4.0
ebooklib>=0.18
beautifulsoup4>=4.12.0

# ── Testing ---------------------------------------------------------------
pytest>=8.0.0

# ── LLM & Text Processing -------------------------------------------------
langchain-text-splitters>=0.0.1

# ── MCP Server ------------------------------------------------------------
mcp>=1.0.0
